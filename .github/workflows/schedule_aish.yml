name: Scheduler

run-name: "Aish scheduler in progress"

on:
  workflow_dispatch:
  schedule:
    - cron: '15 12 * 5 *'

jobs:
  hello_world:
    runs-on: ubuntu-latest
    steps:
      - name: Echo current time
        run: echo "The current server time is $(date)"
